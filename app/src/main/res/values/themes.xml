<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CreateBaseProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CreateBaseProject" parent="Base.Theme.CreateBaseProject" />

    <style name="DialogLoading" parent="Theme.AppCompat.Dialog.Alert">
        <item name="android:windowMinWidthMinor">30%</item>
        <item name="android:windowMinWidthMajor">30%</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
</resources>